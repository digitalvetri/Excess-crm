{
  "name": "@excess/cdk",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --require-approval=never",
    "diff": "cdk diff",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf cdk.out dist"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.175.1",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.175.1",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
